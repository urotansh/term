<% if params[:favorite] %>
  $("#note").html("<h2>いいね一覧</h2><%= j(render 'notes', notes: @notes) %>");
<% else %>
  $("#note").html("<h2>投稿一覧</h2><%= j(render 'notes', notes: @notes) %>");
<% end %>
$("#edit").empty()
$("#destroy").empty()
