<%= form_with model: note do |f| %>
  <h2><%= f.label :title %></h2>
  <%= f.text_field :title %>
 
  <h2><%= f.label :content %></h2>
  <%= f.text_area :content, id: "editor" %><br>
  <%= f.submit class: "btn btn-info mb-1"%>
<% end %>
